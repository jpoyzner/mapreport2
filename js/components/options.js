define(['react'], function(React, Detector, Options, Map, Report, Router) {
	return React.createClass({
		render: function() {
			return (
				<div id="mr-options-bucket"><span className="header">&nbsp;MAPREPORT</span></div>
			);
		}
	});
});